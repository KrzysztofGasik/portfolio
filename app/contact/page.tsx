import Contact from './contact';

export const metadata = {
  title: 'Krzysztof Gasik - Contact',
  description: 'Contact me via email (resend)',
};

export default function Page() {
  return <Contact />;
}
